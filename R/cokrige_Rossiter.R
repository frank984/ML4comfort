library(sp)
library(gstat)

data(meuse)
data(meuse.grid)

str(meuse)
str(meuse.grid)

# Select target and co-variable (usually smth with higher sampling density)

# it is possible to use the same set of
# variables as target and co-variables (equal sampling density)


